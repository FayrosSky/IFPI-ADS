#include <stdio.h>

calcular_media(float ){

}

int main(){

    float v[10];

    printf("Digite um numero %f: ", i+1);
    for(int i = 0; i < 10; i++){
        scanf("%f", &v[i]); //mandando o endereço da posição v[i]

    }





    return 0;
}

float calcularMedia(int tam, float* vetor){ //float* vetor observe que é um vetor, apenas de a gente tá passando o ponteiro de um float, vai chegar vários floats aqui, não só um. 
    float media = 0.0;
    for (int i = 0; i < tam; i++){
        somatorio = somatorio / tam;
        return media;
    }
}