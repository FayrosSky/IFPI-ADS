import { question } from "readline-sync";
function main(){
    //Input
    const numA = parseInt(question("Enter a number: "))
    const numB = parseInt(question("Enter another number: "))

    //Processing

    //Output
    console.log(`The numbers (${A}, ${B}) written in reverse order is: (${B}, ${A})`)
    
}
main()