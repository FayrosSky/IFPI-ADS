def main():
    #Input
    base = float(input("Enter the rectangule base length: "))
    height = float(input("Enter the rectangulo height length: "))
    
    #Processing
    area = base * height

    #Output
    print(f"The area of the triangule with base length of {base} and the height lenght of {height} is equal to: {area} a.u ")
main()