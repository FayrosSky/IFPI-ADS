import { get_positive_number, get_text, print } from "../utils/input";

function main(){
    print(`[------ SAMU -------]`)
    let qtd_funcionarios = get_positive_number('Quantidade de Funcionarios: ')

    while(qtd_funcionarios > 0){
        const codigo = get_text()
        const 

        const salario_bruto = calcular_salario(horas, dependentes)
        const inss = salario_bruto
    }
}