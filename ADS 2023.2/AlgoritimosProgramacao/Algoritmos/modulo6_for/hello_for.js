import { question } = require('readline-sync')

function main(){
    const nome = question('nome: ')
    let qtd = Number(question('Número: '))

    for(let i = qtd; i > 0; i--)  //estado inicial, condição de continuidade, trabalho e convergência de dados.
    //for (; i > 0; i--)
    //for (qtd = Number(question('Número: ')))
    //for(let i = 0; i < nome.length; i++)


}