import { get_text } from "../utils/input.js";

function main(){
    let entrada = get_text('Prova/Nadadores "O O": ')
    let dados = entrada.split(' ')
    let prova = Number(dados[0])
    let qtd = Number(dados[1])


    while (true){
        if (prova === 0 && qtd === 0){
            break
        }
        //Trabalho
        entrada = get_text('Prva/Nadadores "0 0": ')
        daods = 
    }
}
